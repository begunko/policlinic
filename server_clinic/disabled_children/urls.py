# disabled_children/urls.py
# from django.urls import path
# from rest_framework.routers import DefaultRouter
# from .views import DisabledChildViewSet

# router = DefaultRouter()
# router.register(r'disabled-children', DisabledChildViewSet)

# urlpatterns = [
#     path('search/', DisabledChildViewSet.as_view({'get': 'list'}), name='disabled-children-search'),
# ] + router.urls
